# Hint

- Inspect `msal-bundle.js`, `auth-context.js`, `config.chunk.js` and `api.js`.
- Find how the frontend decides that an account is authenticated.
- Check whether the backend validates a real identity token or trusts frontend data.
