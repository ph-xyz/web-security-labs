# Hint

- Compare the fields shown in Settings with the full object returned by `/api/v1/me`.
- Capture the profile update request.
- Try sending additional JSON fields instead of changing the endpoint.
