# Hints

## H1
`/api/v1/*` usa cookie. `/api/v2/*` usa Bearer. `/graphql` aceita os dois, mas o console usa Bearer.

## H2
Use `alice` no Autorize e `bob` no navegador para horizontal.

## H3
Use `alice` no Autorize e `root` no navegador para vertical.

## H4
Há um POST admin vulnerável e um POST de refund protegido corretamente. Compare os dois.

## H5
Nem todo 200 é bug: perfil público e `/api/v1/preferences` são esperados.
