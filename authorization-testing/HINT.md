# Hint

- `/api/v1` uses a session cookie; `/api/v2` uses a bearer token.
- Compare Alice with Bob for horizontal access and Alice with Root for vertical access.
- Check response bodies manually: some `200` responses are expected behavior.
